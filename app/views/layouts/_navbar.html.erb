<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/"><i class="fas fa-globe-americas"></i> TravelShop</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to carts_path,class: 'nav-link' do %>Cart <i class="fas fa-shopping-cart"></i><%end%>
        </li>
      <li class="nav-item"> 
        <% if (current_user) then %>
       <%= link_to "Profil", user_path(current_user.id), class:"nav-link" %> 
       <% end %>
      </li> 

      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          Login/out <i class="fas fa-sign-in-alt"></i>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if logged_in? %>
          <a class="dropdown-item" href="../users/sign_out" data-method="delete">Disconnect</a>
          <% else %>
          <% link_to 'Sign Up',new_user_registration_path,class: 'dropdown-item'%>
          <% link_to 'Log In' ,new_user_session_path,class: 'dropdown-item'%>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
